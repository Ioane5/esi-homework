package com.example.sales.domain.model;

public enum POStatus {
    PENDING,
    ACCEPTED,
    REJECTED,
    PLANT_DISPATCHED,
    PLANT_DELIVERED,
    PLANT_REJECTED_BY_CUSTOMER,
    PLANT_RETURNED,
    INVOICED
}
